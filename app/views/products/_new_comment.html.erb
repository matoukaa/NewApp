<% if signed_in? %>
	<%= form_for([@product, @product.comments.build], remote: true) do |f| %>
		<p>
			<%= f.label :body, "Customer Reviews" %><br>
			<%= f.text_area :body, class: "form-control" %>
		</p>
		<p>
			<div class="rating"></div>
		</p>
		<p>
			<%= f.submit "Submit", class: "btn btn-default", type: "submit" %>
		</p>
	<% end %>
<% end %>